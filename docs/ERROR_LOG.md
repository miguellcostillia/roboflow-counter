# Error Log (Last issues / traces)

Append the latest relevant errors here so the next session can pick up quickly.

## 2025-11-02
- Typer: `No such option: --url` → fixed by using positional arg for URL.
- `git apply` corrupt at line 50 → avoided by GitHub branch/PR workflow; use code inserter if needed.


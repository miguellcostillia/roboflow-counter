#!/usr/bin/env bash
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "❌ Commits auf 'main' sind gesperrt. Bitte Feature-Branch + PR-Workflow verwenden."
  echo "   Tipp: nb feat <titel> ; prup ; finish_branch"
  exit 1
fi
exit 0

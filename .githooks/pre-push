#!/usr/bin/env bash
protected_branch="main"
while read local_ref local_sha remote_ref remote_sha; do
  branch="${remote_ref#refs/heads/}"
  if [ "$branch" = "$protected_branch" ]; then
    echo "❌ Push auf '$protected_branch' ist blockiert. PR-Workflow nutzen."
    exit 1
  fi
done
exit 0

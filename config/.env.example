# .env.example — kopiere zu config/.env und fülle Secrets aus
# Diese Werte überschreiben YAML, falls in Code so vorgesehen (ENV > YAML).

# Roboflow / Inferenz
ROBOFLOW_API_KEY=rf_xxx_your_key_here

# RTSP / Streams (falls Auth nötig)
RTSP_USERNAME=
RTSP_PASSWORD=

# Optional: InfluxDB Metrics
INFLUX_TOKEN=

# Optional: OPC-UA / weitere Integrationen
OPCUA_ENDPOINT=
OPCUA_USERNAME=
OPCUA_PASSWORD=
